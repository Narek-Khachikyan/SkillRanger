{
  "schemaVersion": "1.0",
  "name": "backend-auth-v1",
  "targetCounts": { "triggerPrompts": 0, "taskEvals": 2 },
  "triggerPrompts": [],
  "taskBands": [
    {
      "id": "auth",
      "targetCount": 2,
      "seedTasks": [
        {
          "id": "auth-env-handling",
          "prompt": "Scaffold NextAuth with env-based secret handling",
          "assertions": [{ "text": "Secrets via process.env, no hardcoded", "graderType": "test" }]
        },
        {
          "id": "auth-secret-audit",
          "prompt": "Audit for leaked credentials and dotenv exposure",
          "assertions": [{ "text": "No leaked credentials", "graderType": "schema" }]
        }
      ]
    }
  ],
  "scoring": { "utilityWeights": { "functionalCorrectness": 1 }, "promotionGates": {} }
}
